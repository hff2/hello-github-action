name: Trigger When Finsh Release
run-name: Test Direct Release
on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Do something
      run: echo "A new release was published!"
